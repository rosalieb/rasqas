% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compute_statistic.R
\name{compute_statistic}
\alias{compute_statistic}
\title{Compute parameter statistics over a given period}
\usage{
compute_statistic(
  df,
  date_column,
  value_column,
  year,
  start_doy,
  end_doy,
  statistic = c("min", "max", "mean", "median"),
  min_data_threshold,
  use_exception = FALSE,
  exception_id = NULL,
  exception_list = NULL
)
}
\arguments{
\item{df}{A data.frame containing at least a date column and a temperature column.}

\item{date_column}{Column name or index containing date-time values
(expected format: "yyyy-mm-dd hh:mm").}

\item{value_column}{Column name or index containing temperature values.}

\item{year}{Integer. Year to filter the data.}

\item{start_doy}{Integer. First Julian day of the analysis period.}

\item{end_doy}{Integer. Last Julian day of the analysis period.}

\item{statistic}{Character. One of "min", "max", "mean", "median".}

\item{min_data_threshold}{Numeric between 0 and 1. Minimum proportion of days
with available data required to perform the calculation.}

\item{use_exception}{Logical. If TRUE, allows bypassing the data threshold check.}

\item{exception_id}{Vector identifying the current case (e.g., c("LakeName", year)).}

\item{exception_list}{List of allowed exceptions.}
}
\value{
A data.frame with one row and the following columns:
\describe{
\item{statistic}{Type of statistic computed}
\item{value}{Computed temperature value (rounded to 1 decimal)}
\item{date}{Date of occurrence (only for min/max, otherwise NA)}
\item{data_availability}{Proportion of days with available data}
\item{comment}{Additional information (e.g., missing data warning)}
}
}
\description{
Computes temperature statistics (maximum, minimum, mean, or median)
over a specified Julian day period within a given year.
Also returns the date of occurrence for min/max values when relevant.
}
\details{
The function filters the dataset for a given year and a range of Julian days
(e.g., summer period), then computes the requested statistic if sufficient
data are available. A minimum data availability threshold is required unless
explicitly overridden using the exception mechanism.
}
\examples{
\dontrun{
compute_statistic(data, "date", "temp", 2020, 150, 250, "max", 0.8)
}

}
\author{
Raphaelle Napoleoni \cr
Rosalie Bruel
}
