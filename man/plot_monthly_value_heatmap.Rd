% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_monthly_value_heatmap.R
\name{plot_monthly_value_heatmap}
\alias{plot_monthly_value_heatmap}
\title{Plot monthly value heatmap by depth or sensor}
\usage{
plot_monthly_value_heatmap(
  df,
  station = NULL,
  station_col = "cd_station",
  date_col = "startDate",
  depth_col = "Profondeur_m",
  value_col = "Temperature_moyenne_mensuelle",
  nobs_col = "nombre_observation",
  min_obs = 25,
  depth_breaks = c(0, 10, 25),
  depth_values = NULL,
  depth_tolerance = 0.2,
  facet_type = "wrap",
  facet_rows = NULL,
  facet_cols = NULL,
  language = "fr"
)
}
\arguments{
\item{df}{A data frame containing monthly temperature data.}

\item{station}{Character. Station identifier used for filtering.}

\item{station_col}{Column name for station (character).}

\item{date_col}{Column name for the date (character).}

\item{depth_col}{Column name for depth (character).}

\item{value_col}{Column name for value (character). Default plot is
set for temperature, so labels will reflect that. The output being
a ggplot object, the user can then modify the labels outside the
function.}

\item{nobs_col}{Column name for number of observations (character).}

\item{min_obs}{Minimum number of observations required (default = 25).}

\item{depth_breaks}{Numeric vector for layer mode
(e.g. c(0, 10, 30) → 0–10, 10–30, 30–max).}

\item{depth_values}{Numeric vector for sensor mode (optional)
(e.g. c(0.5, 1.5, 10.5) will visualize only sensors at these depths).}

\item{depth_tolerance}{Numeric tolerance for matching depths}

\item{facet_type}{"wrap" or "grid"}

\item{facet_rows}{Column name for facet rows (grid only)}

\item{facet_cols}{Column name for facet columns (grid only)}

\item{language}{Character. "fr" (default) or "en" for labels.}
}
\value{
A ggplot object.
}
\description{
This function creates a heatmap of monthly mean values across years,
with flexible depth handling:
\itemize{
\item depth intervals (layer mode)
\item specific sensor depths (sensor mode)
}
}
\details{
Depth intervals are automatically extended to the maximum observed depth.
Labels are adapted depending on the selected language.
}
\author{
Rosalie Bruel
}
