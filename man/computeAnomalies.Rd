% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/computeAnomalies.R
\name{computeAnomalies}
\alias{computeAnomalies}
\title{Calculate anomalies relative to a reference period}
\usage{
computeAnomalies(
  x,
  y,
  yr_start,
  yr_end,
  duration = 60,
  date,
  fun = list(mean = mean, max = max, min = min)
)
}
\arguments{
\item{x}{A vector of dates (class \code{Date}) at daily resolution.}

\item{y}{A numeric vector of the parameter of interest, same length as \code{x}.}

\item{yr_start}{Integer. Start year (YYYY) of the reference period.}

\item{yr_end}{Integer. End year (YYYY) of the reference period.}

\item{duration}{Integer. Number of days prior to \code{date} used to compute
summary statistics. Default is 60 (about 2 months).}

\item{date}{A \code{Date} or character string coercible to \code{Date}
(format \code{YYYY-MM-DD}) corresponding to the event of interest
(e.g. sampling date).}

\item{fun}{A named list of functions to apply to \code{y} over the comparison
window. Default is \code{list(mean = mean, max = max, min = min)}.}
}
\value{
A tibble with one row per summary function, containing:
\itemize{
\item \code{funs}: name of the summary function,
\item \code{reference}: value over the reference period,
\item \code{sampling_year}: value for the sampling year,
\item \code{anomaly}: difference between sampling year and reference,
\item \code{ref_start}, \code{ref_end}: reference period bounds,
\item \code{sampling_date}: sampling date,
\item \code{duration_comparison_days}: length of the comparison window.
}
}
\description{
Computes anomalies for a parameter by comparing summary statistics
calculated over a reference period to those calculated for the sampling
year, over a fixed window of days preceding a given date.

The comparison window is defined by aligning calendar days across years
and selecting the \code{duration} days prior to \code{date}.
}
\details{
For each function in \code{fun}, the function is applied to:
\itemize{
\item the reference period (years between \code{yr_start} and \code{yr_end}),
\item the sampling year (year of \code{date}).
}

The anomaly is computed as:
\deqn{anomaly = sampling\_year - reference}
}
\examples{
\dontrun{
dates <- seq(as.Date("1990-01-01"), as.Date("2020-12-31"), by = "day")
values <- rnorm(length(dates))

computeAnomalies(
  x = dates,
  y = values,
  yr_start = 1991,
  yr_end = 2010,
  date = "2020-06-15",
  duration = 60
)
}

}
\author{
Rosalie Bruel
}
