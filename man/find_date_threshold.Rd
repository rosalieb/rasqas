% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/find_date_threshold.R
\name{find_date_threshold}
\alias{find_date_threshold}
\title{Find first date where a difference exceeds or is below a threshold}
\usage{
find_date_threshold(
  df,
  date_column,
  value1_column,
  value2_column = NULL,
  which_value_to_subtract = 1,
  threshold = 0,
  confirmation_margin = 0,
  duration = 1,
  direction = "above",
  error_tolerated = 1,
  absolute_value = FALSE,
  min_data_availability = 0.9
)
}
\arguments{
\item{df}{A data.frame containing the data.}

\item{date_column}{Column name or index containing date-time values.
Accepted formats: "\%Y-\%m-\%d \%H:\%M:\%S" or "\%Y-\%m-\%d".}

\item{value1_column}{Column name or index for the first variable.}

\item{value2_column}{Column name or index for the second variable (optional).
If NULL, comparison is done against 0.}

\item{which_value_to_subtract}{Integer (1 or 2). Defines the subtraction order:
\itemize{
\item 1: Value1 - Value2 (default)
\item 2: Value2 - Value1
}}

\item{threshold}{Numeric. Minimum value the confirmation_margin must exceed.}

\item{confirmation_margin}{Numeric. Additional margin required at the end of the duration.
Useful to confirm the signal (default = 0).}

\item{duration}{Integer. Number of consecutive observations required (default = 1).}

\item{direction}{Character string specifying the detection direction.
One of:
\itemize{
\item \code{"above"} (default): detects the first date where the value exceeds \code{threshold}.
\item \code{"below"}: detects the first date where the value is less than or equal to \code{threshold}.
}
When \code{confirmation_margin > 0}, the last observation of the detection window
must exceed \code{threshold + confirmation_margin} (\code{"above"}) or be below
\code{threshold - confirmation_margin} (\code{"below"}).}

\item{error_tolerated}{Numeric between 0 and 1. Proportion of values within the
duration that must satisfy the threshold condition (default = 1).}

\item{absolute_value}{Logical. If TRUE, uses absolute confirmation_margin.}

\item{min_data_availability}{Numeric between 0 and 1. Minimum proportion of
expected days with observations required to compute the exceedance duration.}
}
\value{
A data frame with:
\describe{
\item{date_result}{First date where condition is met}
\item{start_date}{First date in dataset}
\item{end_date}{Last evaluated date}
\item{threshold}{Threshold used}
\item{confirmation_margin}{Additional margin used}
\item{duration}{Duration used}
\item{error_tolerated}{Tolerance level}
\item{comment}{Information about execution}
\item{date_found}{Logical indicating success}
}
}
\description{
Identifies the first date where the difference between two variables exceeds
or is below a specified threshold for a given duration. This can be used, for
example, to detect inversion events (e.g., when surface temperature becomes
higher than deep temperature).
}
\details{
The function computes the confirmation_margin between two variables and detects the
first occurrence where this confirmation_margin:
\itemize{
\item exceeds a given threshold,
\item persists over a specified duration,
\item satisfies a tolerance level for missing/non-compliant values,
\item and optionally exceeds an additional confirmation margin (\code{confirmation_margin}).
}

The function also reports simple data-quality metrics over the analysed
period, including:
\itemize{
\item the expected number of days,
\item the number and proportion of days with available data,
\item whether the detected event occurs on the first or last day with
available data.
}

If \code{value2_column} is NULL, the function compares \code{value1_column} to 0.
}
\examples{
\dontrun{
find_date_threshold(df, "date", "temp_surface", "temp_bottom",
                    threshold = 0, duration = 5)
}

}
\author{
Rosalie Bruel
}
