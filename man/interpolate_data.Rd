% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/interpolate_data.R
\name{interpolate_data}
\alias{interpolate_data}
\title{Interpolate data along depth intervals and plot interpolated temperature heatmap over depth and time}
\usage{
interpolate_data(
  df,
  date_col,
  depth_col,
  value_col,
  depth_interval = 0.1,
  datetime = FALSE,
  average_close_depths = TRUE
)
}
\arguments{
\item{df}{A data frame or tibble containing at least date, depth, and value columns.}

\item{date_col}{Name of the date column (character).}

\item{depth_col}{Name of the depth column (character).}

\item{value_col}{Name of the variable to interpolate and plot (character).}

\item{depth_interval}{Numeric. Resolution of the depth grid (default = 0.1).}

\item{datetime}{Logical. If TRUE, treats date_col as POSIXct/datetime (default = FALSE).}

\item{average_close_depths}{Logical. If TRUE (default), values measured at depths
closer than \code{depth_interval} are averaged before interpolation.}
}
\value{
A list with:
\describe{
\item{data}{The interpolated dataset}
\item{plot}{A ggplot2 heatmap-style plot}
}
}
\description{
This function interpolates values (e.g., temperature) along a depth gradient
for each date and produces a heatmap-style plot. It is designed for datasets
like vertical water column measurements with irregular depth sampling.
}
\details{
The function:
\itemize{
\item Expands the dataset to a full grid of Date × Depth
\item Optionally averages values measured at very similar depths (e.g. 0.50 and 0.51 m)
\item Performs linear interpolation (\code{stats::approx}) along depth for each date
\item Requires at least 2 non-missing values per date to interpolate
}

When \code{average_close_depths = TRUE}, the function detects if multiple measurements
fall within the same depth interval and averages them. A message is displayed
when this occurs.
}
\author{
Rosalie Bruel
}
